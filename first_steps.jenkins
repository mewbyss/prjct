#!groovy
// Check ub1 properties
pipeline {
    agent any
	
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
	
	stages {
		stage ('Heresy') {
			steps {
				echo "Start"
				echo "Processing..."
				echo "The end"
		}}
	}
    }

